{{#each columns as |column|}}
	{{component column.headerCellTemplateName
	    column=column
	    height=height
  		columnMode=columnMode
  		columnsFillTable=columnsFillTable
  		toggleTableCollapse='toggleTableCollapse'
	}}
{{/each}}
